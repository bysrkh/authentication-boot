spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: security-boot
    profiles:
      active:
        default
    cloud:
      config:
        uri: http://localhost:8888